import {
  systemPalette,
  systemStatus,
  systemGradients,
  systemTypography,
  systemSpacing,
  systemRadius,
  systemShadows,
} from '../design/system';

export const theme = {
  colors: {
    background: systemPalette.background,
    backgroundAlt: systemPalette.backgroundAlt,
    surface: systemPalette.surface,
    surfaceMuted: systemPalette.surfaceAlt,
    surfaceTint: systemPalette.surfaceTint,
    surfaceGlass: systemPalette.surfaceGlass,
    primaryText: systemPalette.textPrimary,
    secondaryText: systemPalette.textSecondary,
    mutedText: systemPalette.textMuted,
    disabledText: systemPalette.textDisabled,
    inverseText: systemPalette.textInverse,
    accent: systemPalette.accent,
    accentSoft: 'rgba(27, 169, 122, 0.16)',
    primary: systemPalette.primary,
    info: systemPalette.info,
    warning: systemPalette.warning,
    error: systemPalette.error,
    success: systemPalette.success,
    badge: systemPalette.badge,
    border: systemPalette.border,
    borderMuted: systemPalette.borderMuted,
    divider: systemPalette.borderMuted,
    overlay: systemPalette.overlay,
    icon: systemPalette.icon,
  },
  status: {
    ...systemStatus,
  },
  tints: {
    accent: 'rgba(27, 169, 122, 0.16)',
    primary: 'rgba(45, 108, 246, 0.12)',
    info: 'rgba(90, 163, 255, 0.16)',
    warning: 'rgba(226, 152, 51, 0.16)',
    error: 'rgba(249, 95, 95, 0.18)',
    success: 'rgba(27, 169, 122, 0.18)',
  },
  gradients: {
    background: systemGradients.background,
    weather: systemGradients.weather,
    action: systemGradients.action,
  },
  spacing: systemSpacing,
  typography: systemTypography,
  radii: systemRadius,
  borderRadius: systemRadius,
  shadows: {
    cardSoft: systemShadows.cardSoft,
    card: systemShadows.card,
    cardStrong: systemShadows.cardStrong,
  },
};
